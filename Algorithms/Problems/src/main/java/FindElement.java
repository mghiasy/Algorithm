public class FindElement {
    //Suppose the array arr is already known to be in sorted order, Determine whether z is in arr
    public static boolean find(int[] arr, int z) { //implement
        return true;
    }
}
